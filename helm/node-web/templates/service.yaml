apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-web.fullname" . }}
  labels:
    {{- include "node-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "node-web.selectorLabels" . | nindent 4 }}
